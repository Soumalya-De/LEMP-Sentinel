# ⚠️ TEMPORARY TRIVY SUPPRESSIONS - ACTIVE REMEDIATION REQUIRED
#
# TRACKING ISSUE: https://github.com/Soumalya-De/LEMP-Sentinel/issues/XXX
# 
# Reason: The following libxml2 CVEs are present in Alpine Linux 3.19/3.20 base image.
# These are upstream distro vulnerabilities that will be fixed when Alpine releases libxml2 2.13.9-r0.
# 
# REMEDIATION TIMELINE:
# - Check weekly: https://pkgs.alpinelinux.org/package/edge/main/x86_64/libxml2
# - Target: Remove suppressions within 2 weeks of Alpine 3.23 stable release
# - Last checked: 2025-11-07
#
# SUPPRESSION JUSTIFICATION:
# - Risk accepted: These CVEs require specific attack vectors (XML parsing exploitation)
# - Mitigation: Application does not accept untrusted XML input from external sources
# - Temporary: Will be removed once upstream fix is available
#
# ============================================================================

# libxml2: Heap Use-After-Free (HIGH severity)
# Fixed in: libxml2 2.13.9-r0 (awaiting Alpine 3.23 release)
# Tracking: https://github.com/Soumalya-De/LEMP-Sentinel/issues/XXX
CVE-2025-49794

# libxml2: Type confusion DoS (MEDIUM severity)
# Fixed in: libxml2 2.13.9-r0 (awaiting Alpine 3.23 release)
# Tracking: https://github.com/Soumalya-De/LEMP-Sentinel/issues/XXX
CVE-2025-49796

# libxml2: Null pointer dereference DoS (MEDIUM severity)
# Fixed in: libxml2 2.13.9-r0 (awaiting Alpine 3.23 release)
# Tracking: https://github.com/Soumalya-De/LEMP-Sentinel/issues/XXX
CVE-2025-49795

# libxml2: Integer overflow → stack buffer overflow (HIGH severity)
# Fixed in: libxml2 2.13.9-r0 (awaiting Alpine 3.23 release)
# Tracking: https://github.com/Soumalya-De/LEMP-Sentinel/issues/XXX
CVE-2025-6021

# ============================================================================
# ACTION REQUIRED: Remove all entries above after upgrading PHP_BASE_IMAGE
# ============================================================================# Add SHA256 IDs or vulnerability IDs (e.g., CVE-XXXX-YYYY) to suppress known/acceptable findings.
# Include a rationale above each rule and a link to an issue/PR tracking remediation.
# Example:
#
# # alpine:3.18 busybox CVE that does not affect our usage path
# CVE-2023-12345
